<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QLCT_N3"
        tools:targetApi="31">
        <activity
            android:name=".View.history_sharing.HistoryShareMoneyActivity"
            android:exported="false" />
        <activity
            android:name=".View.shareing_detail.MoneySharingDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".View.searchmoney.ShareMoneyActivity"
            android:exported="false" />
        <activity
            android:name=".View.add_directory.AddDirectoryActivity"
            android:exported="false" />
        <activity
            android:name=".View.edit_directory.EditDirectoryActivity"
            android:exported="false" />
        <activity
            android:name=".View.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>